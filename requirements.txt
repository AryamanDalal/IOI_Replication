# Top-level dependencies for the IOI / GPT-2 replication.
# Pinned to the versions that installed and passed smoke_test.py cleanly
# (Python 3.11, macOS). For a byte-exact environment use requirements.lock.txt.
#
#   python3.11 -m venv .venv
#   ./.venv/bin/python -m pip install -r requirements.txt

transformer_lens==3.4.0
torch==2.7.1
transformers==5.11.0   # transformer_lens pulls this; pinned because TL is sensitive to it
einops==0.8.2
numpy==2.4.6
pandas==3.0.3
matplotlib==3.10.9
jupyter==1.1.1
circuitsvis==1.43.3
